{
  "name": "risen",
  "private": true,
  "type": "module",
  "description": "RISEN — a turn-based browser roguelite. The game is index.html + js/ + assets/; everything here exists only to test it.",
  "scripts": {
    "test": "node tests/run.mjs",
    "setup": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
